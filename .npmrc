# Dev-time pnpm settings for this workspace: local stores so installs and the
# pack-dist pipeline never touch user-global caches (sandbox-friendly and
# reproducible). Distribution manifests are rewritten by scripts/pack-dist.mjs
# and never contain these paths.
store-dir=.tools/pnpm-store
cache-dir=.tools/npm-cache
